#include<stdio.h>

void add(int a, int b) // a = 10 ,b = 20 // c will compile from top to buttom
{
int sum = a+b;
printf("\n sum of %d and %d is : %d",a,b,sum);
}
void sub(int a, int b) 
{
int sub = a-b;
printf("\n sub of %d and %d is : %d",a,b,sub);
}
void mult(int a, int b) 
{
int mult = a*b;
printf("\n mult of %d and %d is : %d",a,b,mult);
}
void div(int a, int b) 
{
int div = a/b;
printf("\n div of %d and %d is : %d",a,b,div);
}
int main() // user defined
{
 int n1 = 10 ,n2 = 20;
printf("main start"); // pre defined
 add(n1,n2);
add(20,30);
add(30,40);
add(40,50);
sub(n1,n2);
sub(30,10);
sub(50,30);
sub(20,30);
mult(n1,n2);
mult(10,20);
mult(20,20);
mult(30,20);
div(n1,n2);
div(20,10);
div(30,10);
div(40,10);
return 0;
}

output : 
main start
 sum of 10 and 20 is : 30
 sum of 20 and 30 is : 50
 sum of 30 and 40 is : 70
 sum of 40 and 50 is : 90
 sub of 10 and 20 is : -10
 sub of 30 and 10 is : 20
 sub of 50 and 30 is : 20
 sub of 20 and 30 is : -10
 mult of 10 and 20 is : 200
 mult of 10 and 20 is : 200
 mult of 20 and 20 is : 400
 mult of 30 and 20 is : 600
 div of 10 and 20 is : 0
 div of 20 and 10 is : 2
 div of 30 and 10 is : 3
 div of 40 and 10 is : 4

=== Code Execution Successful ===
===============================================================================================================================
#include<stdio.h>

void(int a, int b); //function declaration // c will compile from top to buttom
void sub(int a, int b) ;
void mult(int a, int b);
void div(int a, int b) ;
int main() // user defined
{
 int n1 = 10 ,n2 = 20;
printf("main start"); // pre defined
 add(n1,n2);
add(20,30);
add(30,40);
add(40,50);
sub(n1,n2);
sub(30,10);
sub(50,30);
sub(20,30);
mult(n1,n2);
mult(10,20);
mult(20,20);
mult(30,20);
div(n1,n2);
div(20,10);
div(30,10);
div(40,10);
return 0;
}
void add(int a, int b) // a = 10 ,b = 20 // c will compile from top to buttom // function defination
{
int sum = a+b;
printf("\n sum of %d and %d is : %d",a,b,sum);
}
void sub(int a, int b) 
{
int sub = a-b;
printf("\n sub of %d and %d is : %d",a,b,sub);
}
void mult(int a, int b) 
{
int mult = a*b;
printf("\n mult of %d and %d is : %d",a,b,mult);
}
void div(int a, int b) 
{
int div = a/b;
printf("\n div of %d and %d is : %d",a,b,div);
}
===============================================================================================================================
#include<stdio.h>
int square(int n)
{
   
    printf("square : %d", n*n);
    
}
int reverse(int  n)
{
int rev = 0;
int rem;
while(n!=0)
{
rem = n % 10;           // 1234 % 10 = 4 last digit ,3,2,1
rev = rev * 10 + rem;   // 0*10 + 4 = 4,3,2,1
n /= 10;                // n=n/10 
}
printf("reverse : %d",rev);

}
int main()
{
int n;
printf("ENTER A NUMBER : ");
scanf("%d",&n);
reverse(n);
printf("\n");
square(n);
return 0;
}

output : 

ENTER A NUMBER : 12
reverse : 21
square : 144

=== Code Execution Successful ===

#include<stdio.h>
int reverse(int  n)
{
int rev = 0;
int rem;
while(n!=0)
{
rem = n % 10;           // 1234 % 10 = 4 last digit ,3,2,1
rev = rev * 10 + rem;   // 0*10 + 4 = 4,3,2,1
n /= 10;                // n=n/10 
}
printf("reverse : %d",rev);
return rev;
}
int main()
{
int n;
printf("ENTER A NUMBER : ");
scanf("%d",&n);
reverse(n);
return 0;
}

output :

ENTER A NUMBER : 1234
reverse : 4321

=== Code Execution Successful ===
=====================================================================================================================
// adam number
// explanation
// squar of 12 = 144
// reverse of 12 = 21
// squar of 21 = 441
// now squar of 12 and squar of reverse of 12 are reverse of each other. therefore 12 s adam number.



#include <stdio.h>
int reverse(int n){
    int a = n;
    int rev = 0;
    while(a!=0){
        int last = a%10;
        rev = rev*10+last;
        a= a/10;
    }
    return rev;
}


int squar(int n){
    return n*n;
}

int main() {
    printf("Enter a number : ");
   int val;
   scanf("%d",&val);  //12
   int sqr1 = squar(val);//144
   int rev1 = reverse(val); //21   
   int sqr2 = squar(rev1); //441
   int rev2 = reverse(sqr2); //144
   
   if(rev2 == sqr1){
       printf("TRUE because rev2 = %d and sqr1 = %d",rev2, sqr1);
   } else{
       printf("False because rev2 = %d and sqr1 = %d",rev2, sqr1);
   }
  
    return 0;
}

output : 
Enter a number : 12
False because rev2 = 0 and sqr1 = 144

=== Code Execution Successful ===
=====================================================================================================================
